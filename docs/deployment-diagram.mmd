flowchart LR
  subgraph User_Device["Client Browser / Mobile App"]
    UI["Frontend SPA (React / Static Hosting)"]
  end

  subgraph Cloud_Edge["API Gateway Layer"]
    GW["Reverse Proxy / API Gateway (Nginx or Spring Cloud Gateway)"]
  end

  subgraph BackendCluster["Application Tier - Spring Boot Cluster"]
    APP["Petstore Backend (Spring Boot 3.x, Docker Container)"]
    AUTH["Auth Module (JWT Issuer & Session Control)"]
    INVENTORY["Inventory Module (Productos, Notificaciones)"]
  end

  subgraph DataLayer["Data Tier"]
    DB["PostgreSQL (Managed Instance)"]
    Secrets["Secrets Manager (Environment Variables)"]
  end

  subgraph CI_CD["CI/CD Pipeline"]
    GH["GitHub Repository"]
    Runner["GitHub Actions Runner"]
    Registry["Container Registry"]
  end

  %% Connections
  UI -->|HTTPS| GW
  GW -->|JWT Auth + REST/JSON| APP
  APP --> AUTH
  APP --> INVENTORY
  AUTH --> DB
  INVENTORY --> DB
  APP -->|Read Secrets| Secrets

  GH -->|Push| Runner



  Runner -->|Build Image| Registry

  Registry -->|Pull Image| BackendCluster

 
