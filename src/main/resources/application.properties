# ==========================================
# CONFIGURACIÓN DE BASE DE DATOS
# ==========================================
# IMPORTANTE: Las credenciales sensibles se cargan desde variables de entorno
# para mayor seguridad. Ver archivo .env.example para configuración.

spring.datasource.url=${DB_URL:jdbc:postgresql://aws-1-us-east-2.pooler.supabase.com:5432/postgres}
spring.datasource.username=${DB_USERNAME:postgres.bvxlhjnopjvdjliyprpn}
spring.datasource.password=${DB_PASSWORD:peim7wiOri5Xe4RQ}
spring.datasource.driver-class-name=org.postgresql.Driver

# ==========================================
# CONFIGURACIÓN DE JPA/HIBERNATE
# ==========================================
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# ==========================================
# CONFIGURACIÓN DE JWT
# ==========================================
# IMPORTANTE: En producción, usar una clave secreta fuerte generada de forma segura
jwt.secret.key=E#Aa3,q+UHf$Jpcx/1PVR2WelG-9s%0KIj&4yw8.N"MvFQ!Sr)Xd'tD5o7BZLbCu
jwt.expiration.time=${JWT_EXPIRATION_TIME:3600000}

# ==========================================
# CONFIGURACIÓN DE SESIONES
# ==========================================
# Tiempo de expiración por inactividad en milisegundos (15 minutos por defecto)
session.inactivity.timeout=${SESSION_INACTIVITY_TIMEOUT:900000}
